# Prompt: Разработка Backend для AirTrace RU (FastAPI)

**Роль:** Ты — Senior Python Developer. Твоя задача — спроектировать и написать архитектуру бэкенда для приложения мониторинга качества воздуха в России.

## 🛠 Технологический стек
- **Язык:** Python 3.10+
- **Фреймворк:** FastAPI
- **Библиотеки:** `httpx` (для асинхронных запросов), `pydantic` (для валидации)
- **Источник данных:** Open-Meteo Air Quality API

## 📋 Техническое задание

### 1. Эндпоинты
Реализуй следующие эндпоинты:
- `GET /weather/current?lat={lat}&lon={lon}` — получение текущих данных.
- `GET /weather/forecast?lat={lat}&lon={lon}` — прогноз на 24 часа.
- `GET /health` — проверка работоспособности сервиса.

### 2. Логика обработки данных (Air Quality Index)
Поскольку API возвращает сырые данные (мкг/м³), необходимо реализовать конвертер в индекс **AQI** (по европейскому стандарту или стандарту США), чтобы фронтенд получал понятный статус.
- **Показатели:** PM2.5, PM10, NO2, SO2.
- **Выходные данные:** Индекс (0-500), цветовой код (green, yellow, red и т.д.) и текстовый совет (на русском языке).

### 3. Специфика для РФ
Добавь в логику функцию `check_nmu_risk(city_name)`, которая на основе текущих данных SO2 и скорости ветра определяет риск возникновения режима «Черного неба» (Неблагоприятных метеоусловий).

### 4. Приватность
- Не сохраняй логи с координатами пользователей.
- Реализуй кэширование ответов от внешнего API (например, на 15 минут), чтобы не делать лишних запросов и не передавать данные пользователя слишком часто.

## 📁 Структура проекта
Создай структуру файлов:
- `main.py` — точка входа и эндпоинты.
- `schemas.py` — Pydantic модели для валидации запросов и ответов.
- `services.py` — логика запросов к Open-Meteo.
- `utils.py` — формулы расчета AQI и текстовые рекомендации.

## 🚀 Инструкция по генерации кода
1. Напиши сначала `schemas.py`, опираясь на структуру JSON от Open-Meteo.
2. Реализуй асинхронный клиент в `services.py` через `httpx`.
3. Напиши логику в `utils.py`, которая переводит концентрацию веществ в понятные человеку советы: "Можно проветривать", "Закройте окна", "Опасно для здоровья".
4. Собери все в `main.py`.

**Важно:** Весь код должен быть снабжен комментариями на русском языке и использовать асинхронность (`async/await`).